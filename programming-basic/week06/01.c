#include <stdio.h>

// 기본자료형
int main(){
    int i=10;
    char a='a';
    float f=10.1;
    double d=11.1;

    printf("size of i = %d\n",sizeof(i));
    printf("size of a = %d\n",sizeof(a));
    printf("size of f = %d\n",sizeof(f));
    printf("size of d = %d\n",sizeof(d));

    return 0;
}
