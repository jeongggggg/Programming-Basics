#include <stdio.h>

/*
    다음의 구조체 info를 선언해보세요.
    - number : 번호를 나타내는 정수값(int)
    - name : 이름을 나타내는 문자열(size = 20)
    - grade : 학점을 나타내는 실수(float)
*/

struct info {
    int number;
    char name[20];
    float grade;
};
